import dotenv from 'dotenv';

dotenv.config();
export const { PORT } = process.env;
export const { NODE_ENV } = process.env;
export const { API_PATH } = process.env;
export const { FLW_PUB_KEY } = process.env;
export const { FLW_SEC_KEY } = process.env;
export const { FLW_ENC_KEY } = process.env;
export const { FLW_API_URL } = process.env;
export const { FLW_WBHK_HASH } = process.env;
